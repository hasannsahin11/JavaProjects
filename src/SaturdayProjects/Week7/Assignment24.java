package SaturdayProjects.Week7;

public class Assignment24 {
    public static void main(String[] args) {
        // We already know the index so we just need to assign idx to index of 20 of the array
        int[] arr = {10, 20, 30};
        int size = arr.length;
        int idx = arr[2];
        System.out.println(idx);
    }
}